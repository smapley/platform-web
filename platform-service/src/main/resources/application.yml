spring:
  application:
    name: platform-server #服务名称

eureka:
  instance:
    hostname: localhost #配置主机名
  client:
    register-with-eureka: false #服务注册中心是否以自己为客户端进行注册
    fetch-registry: false #是否取得注册信息

server:
  port: 9000 #端口号
